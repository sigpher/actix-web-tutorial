[workspace]
resolver = "2"
members = ["s1"]

[workspace.dependencies]
actix-web = {version = "*",features = ["openssl"]}
tokio = {version = "*",features = ["full"]}
num_cpus = "*"
openssl = {version = "*"}
